package com.gildedrose.adapter.event;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;


class DomainEventPersistListenerTest {

    //  @Autowired
    //  private DomainEventPersistListener domainEventPersistListener;

    //   @Autowired
    //   private TestEntityManager entityManager; //Needed if you test if event is persisted in database

    @Test
    @Disabled
    void givenDomainEvent_whenPersistDomainEvent_thenEventIsPersistedinDB() {


        /*
        Logic to query the database via the testEntityManager
        and do an assertions of it to verify if the item event is persisted
         */
    }


}
